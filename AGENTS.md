# 项目协作要求

## UI 修改必读

修改或新增任何页面、卡片、导航、表单、筛选、弹窗前，必须完整阅读 [统一 UI 规范](docs/ui-style-guide.md)，并严格按其中的配色、字号、字重、间距、圆角、状态和交互要求实施。

- 首页、开仓记录页及底部导航是当前设计基准；其他历史页面不作为新的样式样板。
- 保留用户及其他任务的已有改动，只修改当前任务涉及的内容。
- 用户新的明确指示优先于规范；若指示改变设计标准，同步更新规范。
- UI 润色不得意外改变开仓数量、杠杆、止损止盈和实时价格的业务语义。

## 验证和交付

用户已明确要求：后续默认编译通过即可。常规界面修改只执行 `flutter build apk --debug` 进行构建验证，不默认运行 `flutter analyze`、整套测试或新增 UI 测试。遵循用户后续的明确验证要求。

编译成功不等于经过真机或截图视觉验收；说明验证范围，不夸大结果。交付用简洁中文描述主要变化，提供实际生成的安装包和相关规范文档链接。
